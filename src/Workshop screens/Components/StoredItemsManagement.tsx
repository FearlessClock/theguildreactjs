import Typography from "@mui/material/Typography";
import {Paper} from "@mui/material";

function StoredItemsManagement() {
    return (
        <Paper className="flex w-50 flex-grow">
            <Typography>Stored item information</Typography>
        </Paper>
    )
}

export default StoredItemsManagement;